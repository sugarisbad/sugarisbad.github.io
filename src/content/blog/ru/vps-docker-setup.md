---
title: 'Настройка VPS под Docker: от cloud-init до продакшена'
titleHtml: 'Настройка VPS под Docker: от cloud-init <em>до продакшена</em>'
description: 'Пошагово поднимаем сервер, который не стыдно показать: пользователи и SSH-ключи через cloud-init, Docker с правильным логированием, firewall и автообновления. Всё — воспроизводимо, через код.'
pubDate: 2026-07-12
cat: vps
tags: [vps, docker]
minutes: 9
ctaTitle: 'Некогда настраивать <em>самому</em>?'
ctaText: 'Настроим VPS под Docker под ключ — от 8 400 ₽, с cloud-init-конфигом, который останется у вас.'
---

## Зачем cloud-init, если есть руки

Ручная настройка по SSH работает ровно до второго сервера. cloud-init выполняет конфигурацию при первом старте VPS — пользователи, ключи, пакеты, — и тот же файл поднимет идентичный сервер через год. У всех крупных провайдеров поле «user data» есть прямо в форме создания сервера.

Минимальный конфиг: отдельный пользователь `deploy` с ключом вместо пароля, запрет root-логина, свежие пакеты.

```yaml
#cloud-config
users:
  - name: deploy
    groups: [sudo, docker]
    ssh_authorized_keys:
      - ssh-ed25519 AAAA… you@laptop
    sudo: ALL=(ALL) NOPASSWD:ALL
ssh_pwauth: false
disable_root: true
package_update: true
package_upgrade: true
```

## Docker: официальный репозиторий и лимиты логов

Ставьте Docker из официального репозитория, а не из пакетов дистрибутива — там версии отстают на полгода. И сразу ограничьте логи: контейнер, который пишет в stdout без лимита, за пару месяцев съест весь диск. Это самая частая причина «сервер внезапно умер» из тех, что мы разбираем.

```json
// /etc/docker/daemon.json
{
  "log-driver": "json-file",
  "log-opts": { "max-size": "10m", "max-file": "3" }
}
```

<div class="practice"><span>ИЗ ПРАКТИКИ</span>После перезаписи daemon.json нужен <code>systemctl restart docker</code> — уже запущенные контейнеры сохранят старый лог-драйвер до пересоздания.</div>

## Firewall: три порта, не больше

Наружу смотрят только 22, 80 и 443. Всё остальное — базы, метрики, панели — доступно через SSH-туннель или VPN. Важный нюанс: Docker пишет правила напрямую в iptables в обход ufw, поэтому порт, опубликованный через `-p 5432:5432`, будет открыт всему интернету, что бы ни говорил ufw. Публикуйте порты только на 127.0.0.1.

```bash
$ ufw default deny incoming
$ ufw allow 22/tcp comment 'ssh'
$ ufw allow 80,443/tcp comment 'web'
$ ufw enable
# порты наружу — только через 127.0.0.1:
ports: ["127.0.0.1:5432:5432"]
```

## Чек-лист перед продакшеном

Сервер готов, когда на каждый пункт можно ответить «да» — и показать, где это лежит в коде:

<div class="checklist">
  <div>Вход только по SSH-ключу, root-логин запрещён</div>
  <div>Docker из официального репозитория, логи ограничены (10m × 3)</div>
  <div>ufw: наружу только 22, 80, 443; служебные порты — на 127.0.0.1</div>
  <div>unattended-upgrades включены для security-обновлений</div>
  <div>compose-файлы и cloud-init лежат в git-репозитории</div>
  <div>Бэкапы настроены и хотя бы раз восстановлены</div>
</div>
